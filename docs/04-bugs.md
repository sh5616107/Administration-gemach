# רשימת באגים - תוקנו

## 1. ✅ ולידציה לתעודת זהות
**פתרון**: הוספתי אלגוריתם לבדיקת תקינות מספר זהות ישראלי (Luhn variant) + הגבלה ל-9 ספרות

## 2. ✅ תקופת הלוואה ברירת מחדל
**פתרון**: מוצג בתווית השדה "תאריך החזרה (ברירת מחדל: X חודשים)"

## 3. ✅ תיקיית יצוא אוטומטי
**פתרון**: ברירת מחדל - תיקיית ההורדות. ניתן לשנות בהגדרות

## 4. ✅ העברת חוב לערב
**פתרון**: 
- הערב נרשם אוטומטית כלווה חדש
- נוצרת הלוואה חדשה על שמו
- ההלוואה המקורית מקבלת סטטוס "הועבר לערב" (לא נמחקת)
- הלווה המקורי מתווסף לרשימה השחורה

## 5. ✅ סמל התראות בכניסה
**פתרון**: בדיקת התראות מיידית בטעינה + בדיקה חוזרת כל 30 שניות

## 6. ✅ ולידציה לתאריכים
**פתרון**: 
- תאריך פירעון לא יכול להיות לפני תאריך ההלוואה
- תאריך סיום הפקדה לא יכול להיות לפני תאריך ההפקדה

## 7. ✅ הגדרת הצגת אפשרויות מחזוריות
**פתרון**: 
- הוספתי הגדרה "הצג אפשרויות מחזוריות בטופס הלוואה"
- העברתי את הסעיפים לפני הערבים בטופס

## 8. ✅ עריכה ומחיקה של פירעון
**פתרון**: הוספתי כפתורי עריכה ומחיקה בטבלת הפירעונות + דיאלוג עריכה

## 9. ✅ מניעת מחיקת הלוואה עם פירעונות
**פתרון**: בדיקה לפני מחיקה - אם יש פירעונות, לא ניתן למחוק

## 10. ✅ מניעת מחיקת לווה עם הלוואות פעילות
**פתרון**: בדיקה לפני מחיקה - אם יש הלוואות פעילות, לא ניתן למחוק


## 11. ✅ היסטוריית משיכות הפקדות
**בעיה**: כרגע כשמושכים חלק מהסכום, המערכת לא שומרת היסטוריה של המשיכות - רק את הסטטוס האחרון.

**פתרון**:
1. יצירת טבלה חדשה `depositWithdrawals` לשמירת היסטוריית משיכות
2. כל משיכה נשמרת עם: תאריך, סכום, אמצעי תשלום, הערות
3. עדכון אישור הפקדה להציג:
   - סכום הפקדה מקורי
   - טבלה עם כל המשיכות (תאריך + סכום)
   - סה"כ נמשך
   - יתרה נוכחית
4. עדכון טבלת ההפקדות להציג את סך המשיכות מההיסטוריה
5. תמיכה בגיבוי ושחזור של היסטוריית המשיכות
6. הוספת כפתור "📋 היסטוריה" בטבלת ההפקדות (מופיע רק כשיש משיכות)
7. דיאלוג מפורט להצגת כל המשיכות עם תאריכים, סכומים ואמצעי תשלום

**קבצים שעודכנו**:
- `src/services/database.ts` - הוספת `depositWithdrawalsService`
- `src/components/donations/DepositsTab.tsx` - עדכון לוגיקת משיכות + UI להיסטוריה
- `src/services/documents.ts` - עדכון אישור הפקדה להציג היסטוריה
- `src/pages/Dashboard.tsx` - תמיכה במחיקת כל הנתונים
- `src/pages/AdvancedTools.tsx` - תמיכה בגיבוי/שחזור
- `src/__tests__/depositWithdrawals.test.ts` - טסטים מקיפים (14 טסטים, כולם עוברים ✅)

**סטטוס**: ✅ הושלם - כולל UI מלא להצגת היסטוריה


## 12. ✅ היסטוריית פירעונות ערבים + תזכורת החזר
**בעיה**: 
1. כשערב משלם, רשום רק סה"כ שפרע, אבל לא מתי ובאיזה תאריכים
2. כשלווה פורע אחרי שערב שילם, לא מתריעים שמגיע החזר לערב
3. כשמעדכנים/מוחקים פירעון של לווה, הסטטוס של הלוואת הערב לא מתעדכן

**פתרון**:
1. יצירת טבלה חדשה `guarantorLoanRepayments` לשמירת היסטוריית פירעונות ערבים
2. כל פירעון של ערב נשמר עם: תאריך, סכום, אמצעי תשלום, הערות
3. הוספת פונקציה `recalculateGuarantorLoans` שמחשבת מחדש את מצב הלוואות הערבים
4. קריאה לפונקציה הזו כשמעדכנים או מוחקים פירעון של לווה
5. כשערב שילם יותר מהנדרש, מתווסף לשדה `notes` הודעה: "מגיע החזר לערב: X₪ ⚠️"
6. הסטטוס מתעדכן אוטומטית בהתאם לפירעונות (active/paid)

**קבצים שעודכנו**:
- `src/services/database.ts` - הוספת `guarantorLoanRepaymentsService`
- `src/components/loans/LoansTab.tsx` - הוספת `recalculateGuarantorLoans` וקריאה לה בעדכון/מחיקת פירעון
- `src/components/loans/GuarantorsTab.tsx` - ייבוא `guarantorLoanRepaymentsService`
- `src/pages/Dashboard.tsx` - הוספת מחיקת `guarantorLoanRepayments` ב-"מחק הכל"
- `src/pages/AdvancedTools.tsx` - הוספת `guarantorLoanRepayments` לייצוא/ייבוא


## 13. ✅ מניעת ערבות עצמית
**בעיה**: אדם שרשום גם כלווה וגם כערב יכול להיבחר כערב להלוואה שלו עצמו, מה שלא הגיוני.

**דוגמה**: משה כהן רשום גם כלווה וגם כערב - המערכת מאפשרת לבחור אותו כערב להלוואה שלו.

**פתרון**:
1. הוספת בדיקה ב-`handleGuarantorSelect` ב-`LoansTab.tsx` - בודק אם הערב הוא אותו אדם כמו הלווה (לפי טלפון או ת.ז.)
2. עדכון `checkGuarantorForLoan` ב-`crossCheck.ts` - קבלת `borrowerId` כפרמטר נוסף ובדיקה אם הערב הוא אותו אדם
3. אם מזהים ערבות עצמית - מוצגת הודעת שגיאה: "לא ניתן לבחור את הלווה כערב להלוואה שלו עצמו"
4. הבדיקה מתבצעת בשני שלבים:
   - בצד הלקוח (UI) - מניעה מיידית
   - בצד השרת (crossCheck) - הגנה כפולה

**קבצים שעודכנו**:
- `src/components/loans/LoansTab.tsx` - הוספת בדיקה ב-`handleGuarantorSelect`
- `src/services/crossCheck.ts` - עדכון `checkGuarantorForLoan` לקבל `borrowerId` ולבדוק ערבות עצמית
- `src/__tests__/selfGuarantor.test.ts` - טסטים מקיפים (6 טסטים, כולם עוברים ✅)

**סטטוס**: ✅ הושלם - כולל בדיקה כפולה ומסרי שגיאה ברורים


## 14. ✅ תיקון חישוב כסף עתיד להשתחרר
**בעיה**: הפונקציה `calculateExpectedFunds` בתור ההמתנה הציגה תוצאות לא הגיוניות:
1. הפקדות מחזוריות לא נספרו כלל (חישוב שגוי: `Math.floor(7/30) = 0`)
2. הלוואות גמישות (ללא תאריך פירעון) נכללו בחישוב למרות שאין ודאות מתי יפרעו
3. הלוואות מחזוריות עתידיות (שכבר מחויבות אבל טרם נוצרו) לא נוכו מהצפי

**פתרון**:
1. **הסרת הלוואות גמישות** - הלוואות ללא תאריך פירעון או פירעון מחזורי לא נכללות בחישוב
2. **תיקון חישוב הפקדות מחזוריות** - חישוב מדויק של תאריכי הפקדות עתידיות לפי `recurring_months`
3. **גריעת הלוואות מחזוריות עתידיות** - הלוואות שכבר מחויבות (חלק מסדרה מחזורית) נוכות מהצפי

**דוגמה לתיקון**:
- הלוואה מחזורית 2/5 (עוד 3 הלוואות של 10,000₪ צפויות)
- **לפני**: הצפי לא גרע את ה-30,000₪ המחויבים
- **אחרי**: גורע 30,000₪ מהצפי = תוצאה מדויקת יותר

**קבצים שעודכנו**:
- `src/components/loans/WaitlistTab.tsx` - תיקון `calculateExpectedFunds`
- `src/__tests__/expectedFunds.test.ts` - 13 טסטים מקיפים (כולם עוברים ✅)

**סטטוס**: ✅ הושלם - חישוב מדויק ומציאותי יותר




## 15. ✅ תאריך הלוואה מחזורית לא מחושב נכון
**תאריך:** 31/01/2026
**חומרה:** גבוהה

### תיאור הבעיה:
כאשר יוצרים הלוואה מחזורית חדשה עם `recurring_day` (למשל 15 לחודש), המערכת הייתה יוצרת את ההלוואה הראשונה עם תאריך היום (למשל 31/1), במקום לחשב את תאריך ההלוואה הראשונה לפי ה-`recurring_day`.

**דוגמה לבעיה:**
- היום: 31 בינואר
- recurring_day: 15
- תוצאה שגויה: ההלוואה נוצרת ב-31/1 עם סטטוס `active`
- תוצאה נכונה: ההלוואה צריכה להיווצר ב-15/2 עם סטטוס `planned`

### הסיבה:
ב-`LoansTab.tsx`, כאשר יוצרים הלוואה מחזורית, הקוד לא היה מחשב את תאריך ההלוואה הראשונה לפי ה-`recurring_day`, אלא פשוט השתמש בתאריך היום.

### הפתרון:
1. **הוספת פונקציה `calculateFirstRecurringLoanDate`** ב-`LoansTab.tsx`:
   - מחשבת את תאריך ההלוואה הראשונה לפי `recurring_day`
   - אם `recurring_day` עוד לא הגיע החודש → ההלוואה תהיה החודש
   - אם `recurring_day` כבר עבר החודש → ההלוואה תהיה בחודש הבא
   - טיפול בחודשים קצרים (למשל 31 בפברואר → 28/29 בפברואר)

2. **עדכון לוגיקת יצירת הלוואה מחזורית**:
   - כאשר מסמנים "הלוואה מחזורית", הקוד מאתחל את `recurring_day` ליום הנוכחי
   - כאשר משנים את `recurring_day`, הקוד מחשב אוטומטית את תאריך ההלוואה הראשונה
   - הוספת התראה ויזואלית שמציגה מתי ההלוואה הראשונה תיווצר

3. **הסטטוס נקבע אוטומטית** ב-`database.ts`:
   - אם `loan_date > today` → סטטוס `planned`
   - אם `loan_date <= today` → סטטוס `active`

### טסטים:
נוצר קובץ טסט חדש `recurringLoanDate.test.ts` עם 10 טסטים:
- ✅ תאריך שעוד לא הגיע החודש
- ✅ תאריך שכבר עבר החודש
- ✅ תאריך שהוא היום
- ✅ טיפול בחודשים קצרים (31 בפברואר)
- ✅ טיפול בשנה מעוברת
- ✅ טיפול במעבר שנה
- ✅ יום ראשון בחודש
- ✅ יום אחרון בחודש
- ✅ חודש עם 30 ימים

### קבצים שהשתנו:
- `src/components/loans/LoansTab.tsx` - הוספת פונקציה וחישוב אוטומטי
- `src/__tests__/recurringLoanDate.test.ts` - טסטים חדשים

### תוצאה:
עכשיו כאשר יוצרים הלוואה מחזורית, תאריך ההלוואה הראשונה מחושב נכון לפי `recurring_day`, והסטטוס נקבע אוטומטית (`planned` אם בעתיד, `active` אם היום או בעבר).


## 16. ✅ תאריך הפקדה מחזורית לא מחושב נכון (זהה לבאג #15)
**תאריך:** 01/02/2026
**חומרה:** גבוהה

### תיאור הבעיה:
אותה בעיה כמו בהלוואות מחזוריות - כאשר יוצרים הפקדה מחזורית חדשה עם `recurring_day` (למשל 15 לחודש), המערכת הייתה יוצרת את ההפקדה הראשונה עם תאריך היום (למשל 31/1), במקום לחשב את תאריך ההפקדה הראשונה לפי ה-`recurring_day`.

**דוגמה לבעיה:**
- היום: 31 בינואר
- recurring_day: 15
- תוצאה שגויה: ההפקדה נוצרת ב-31/1 עם סטטוס `active`
- תוצאה נכונה: ההפקדה צריכה להיווצר ב-15/2 עם סטטוס `planned`

### הפתרון:
1. **הוספת פונקציה `calculateFirstRecurringDepositDate`** ב-`DepositsTab.tsx` (זהה ללוגיקה של הלוואות)
2. **הוספת סטטוס `planned` גם להפקדות**:
   - הפקדות עם תאריך עתידי מקבלות סטטוס `planned`
   - הפקדות עם תאריך היום או בעבר מקבלות סטטוס `active`
3. **עדכון הטבלה** להציג סטטוס "מתוכננת" בצבע כחול
4. **הוספת `activatePlannedDeposits`** ב-scheduler כדי להפעיל הפקדות מתוכננות כשהתאריך מגיע
5. **הוספת התראה ויזואלית** בטופס שמציגה מתי ההפקדה הראשונה תיווצר

### קבצים שהשתנו:
- `src/components/donations/DepositsTab.tsx` - הוספת פונקציה, חישוב סטטוס, והתראה ויזואלית
- `src/services/scheduler.ts` - הוספת `activatePlannedDeposits`

### תוצאה:
עכשיו הפקדות מחזוריות מתנהגות בדיוק כמו הלוואות מחזוריות - תאריך מחושב נכון, סטטוס מתאים, וה-scheduler מפעיל אותן בזמן הנכון.


## 17. ✅ הפקדות מתוכננות לא מופיעות בחישוב כספים צפויים
**תאריך:** 01/02/2026
**חומרה:** בינונית

### תיאור הבעיה:
לאחר תיקון באג #16 (הפקדות מחזוריות עם סטטוס `planned`), הפקדות מחזוריות חדשות שנוצרו עם תאריך עתידי לא הופיעו בפירוט הכספים הצפויים להשתחרר בתור ההמתנה.

**הסיבה:**
הפונקציות `calculateExpectedFunds` ו-`prepareExpectedFundsBreakdown` ב-`WaitlistTab.tsx` חיפשו רק הפקדות עם סטטוס `active`:
```typescript
const recurringDeposits = await db.query(
  'SELECT * FROM deposits WHERE is_recurring = 1 AND status = ?', 
  ['active']
)
```

אבל הפקדות מחזוריות חדשות עם תאריך עתידי מקבלות סטטוס `planned` (לפי תיקון באג #16), ולכן לא נכללו בחישוב.

### הפתרון:
עדכון שתי הפונקציות לכלול גם הפקדות עם סטטוס `planned`:
```typescript
const recurringDeposits = await db.query(
  'SELECT * FROM deposits WHERE is_recurring = 1 AND status IN (?, ?)', 
  ['active', 'planned']
)
```

### טסטים:
הוספנו 3 טסטים חדשים ל-`expectedFunds.test.ts`:
- ✅ הפקדה מחזורית עם סטטוס `planned` נכללת בחישוב
- ✅ הפקדות `active` ו-`planned` נכללות ביחד
- ✅ הפקדות עם סטטוס `withdrawn` לא נכללות

סה"כ 23 טסטים עוברים בהצלחה ✅

### קבצים שהשתנו:
- `src/components/loans/WaitlistTab.tsx` - עדכון שתי הפונקציות
- `src/__tests__/expectedFunds.test.ts` - הוספת 3 טסטים חדשים

### תוצאה:
עכשיו הפקדות מחזוריות מופיעות בפירוט הכספים הצפויים, בין אם הן `active` או `planned`.
