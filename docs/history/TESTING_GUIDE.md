# מדריך בדיקה - תיקון הלוואות יתומות

## בדיקה 1: וידוא שהבעיה תוקנה ✓

### בדוק שהסכומים תואמים בדף הבית

1. **פתח את האפליקציה** והיכנס לדף הבית
2. **שים לב לשני מקומות**:
   - **כרטיס "הלוואות פעילות"** - הסכום למעלה
   - **טבלת "לווים פעילים"** - השורה "סה״כ" בתחתית

3. **בדוק התאמה**:
   ```
   כרטיס "הלוואות פעילות": _____ ₪
   סיכום "לווים פעילים":    _____ ₪
   
   ✓ צריכים להיות זהים!
   ```

4. **אם יש הבדל** - זה אומר שיש הלוואות יתומות במערכת
   → עבור לבדיקה 3

---

## בדיקה 2: וידוא שהחסימה עובדת 🛡️

### נסה למחוק לווה עם הלוואה פעילה

1. **עבור לדף "הלוואות"**
2. **בחר לווה שיש לו הלוואה פעילה עם יתרה**
3. **נסה למחוק את הלווה** (כפתור מחיקה)

**תוצאה צפויה**:
```
❌ הודעת שגיאה:
"לא ניתן למחוק לווה עם הלוואה פעילה. 
יש לסגור או להעביר את ההלוואה תחילה."
```

4. **אם התיבה נסגרת בלי שגיאה** - התיקון לא עבד
5. **אם מופיעה השגיאה** - מעולה! ✓

---

## בדיקה 3: איתור הלוואות יתומות קיימות 🔍

### בדוק אם יש הלוואות יתומות כבר במערכת

1. **עבור לדף "כלים מתקדמים"**
2. **בחלק "דוחות"**, לחץ על כפתור **"איתור הלוואות יתומות"**

**תוצאות אפשריות**:

### אפשרות א': אין הלוואות יתומות ✅
```
✓ לא נמצאו הלוואות יתומות
כל ההלוואות משויכות ללווים קיימים במערכת
```
→ מצוין! המערכת תקינה לחלוטין

### אפשרות ב': נמצאו הלוואות יתומות ⚠️
```
סיכום
מספר הלוואות יתומות: 3
סכום כולל: 15,000 ₪

[טבלה עם פרטי ההלוואות]
```

**מה לעשות?**
1. **תעד את הפרטים** - צלם מסך או העתק
2. **זהה את הלווים** - על פי התאריכים/סכומים
3. **בחר דרך טיפול**:

#### דרך 1: שחזור מגיבוי (מומלץ)
- אם יש לך גיבוי לפני המחיקה
- ייבא את הגיבוי או שחזר את רשומת הלווה בלבד

#### דרך 2: יצירת לווה חדש
- עבור ל"הלוואות" → "לווים"
- צור לווה חדש עם הפרטים הנכונים
- שייך את ההלוואה ידנית (דורש עריכת הנתונים)

#### דרך 3: סגירת הלוואה
- אם ההלוואה **שולמה בפועל**
- עבור ל"הלוואות" ומצא את ההלוואה
- הוסף פירעונות עד לסגירת ההלוואה

#### דרך 4: שיוך ללווה אחר
- אם זה אותו אדם עם רשומה כפולה
- ערוך את ההלוואה ושייך למזהה הנכון

⚠️ **חשוב**: לא למחוק הלוואות יתומות! הן מייצגות כסף שיצא מהגמ"ח

---

## בדיקה 4: בדיקה מתקדמת בקונסול 💻

### בדיקה דרך הקונסול (אופציונלי)

1. **פתח את הקונסול** (F12 בדפדפן)
2. **הרץ את הפקודה**:
   ```javascript
   await statsService.findOrphanedLoans()
   ```

**תוצאה צפויה**:
```
🔍 הלוואות יתומות נמצאו: 0
💰 סכום כולל: 0
📋 פרטים: []
```

---

## סיכום בדיקות ✓

- [ ] **בדיקה 1**: הסכומים תואמים בדף הבית
- [ ] **בדיקה 2**: לא ניתן למחוק לווה עם הלוואה פעילה
- [ ] **בדיקה 3**: בדקתי אם יש הלוואות יתומות קיימות
- [ ] **בדיקה 4**: (אופציונלי) בדיקה בקונסול

---

## מה אם משהו לא עובד?

### הסכומים עדיין לא תואמים
1. רענן את הדף (Ctrl+R)
2. נקה את ה-cache (Ctrl+Shift+Del)
3. הרץ בדיקת הלוואות יתומות
4. אם עדיין לא תואם - יש בעיה נוספת

### השגיאה לא מופיעה בעת מחיקה
1. ודא שהבנייה הושלמה (`npm run build`)
2. רענן את הדף
3. בדוק בקונסול אם יש שגיאות JavaScript

### הכפתור "איתור הלוואות יתומות" לא מופיע
1. ודא שאתה בדף "כלים מתקדמים"
2. בדוק שהבנייה הושלמה
3. רענן את הדף

---

## תמיכה
אם נתקעת, בדוק את:
- `ORPHANED_LOANS_FIX.md` - תיעוד מפורט של התיקון
- הקונסול בדפדפן (F12) - לראות שגיאות
- `check-orphaned-loans.js` - סקריפט בדיקה נוסף
