# תיקוני ביקורת - מערכת עמלות

**תאריך:** 10/09/2026  
**סטטוס:** בוצע חלקית, נדרש השלמה ידנית

---

## ✅ תיקונים שבוצעו

### 1. Audit Log - עקביות בקונבנציה
**בעיה:** שימוש בפעולות גנריות (`create`, `update`, `delete`) במקום ספציפיות לישות

**תוקן ב:**
- `src/services/auditLog.ts` - הוחלף ב-`fee_payment_create`, `fee_payment_update`, `fee_payment_delete`, `fee_payment_status_change`
- `src/services/feePaymentsService.ts` - עודכנו כל הקריאות ל-logAudit

### 2. תיקון כפילות Audit ב-updateFeePaymentStatus
**בעיה:** הפונקציה קראה ל-`updateFeePayment` שכבר רושם audit, ואז רשמה audit נוסף

**תוקן:**
- הפונקציה עכשיו עושה UPDATE ישיר ל-DB
- רושמת רק audit אחד עם הפעולה `fee_payment_status_change`
- מונעת כפילות ברישום

---

## ⚠️ תיקונים שנדרשים (לביצוע ידני)

### 3. HTML Escaping למניעת XSS ✋

**בעיה:** `buildFeeReceiptHtml` מזריק ישירות משתני משתמש ל-HTML

**פתרון מוצע:**

```typescript
// להוסיף לפני buildFeeReceiptHtml ב-src/services/documents.ts

/**
 * Escape HTML special characters למניעת XSS
 */
function escapeHtml(text: string): string {
  const div = document.createElement('div')
  div.textContent = text
  return div.innerHTML
}
```

**שדות שצריך escape:**
- `data.borrowerName` → `escapeHtml(data.borrowerName)`
- `data.gemachName` → `escapeHtml(data.gemachName)`
- `data.note` → `escapeHtml(data.note)`
- `data.loanNumber` → `escapeHtml(String(data.loanNumber))`
- `data.receiptNumber` → `escapeHtml(data.receiptNumber)`
- `data.feeType` (אם לא מתורגם) → `escapeHtml(data.feeType)`
- `data.paymentMethod` (אם לא מתורגם) → `escapeHtml(data.paymentMethod)`

**דוגמה:**
```typescript
const safeBorrowerName = escapeHtml(data.borrowerName)
const safeGemachName = escapeHtml(data.gemachName)
const safeNote = data.note ? escapeHtml(data.note) : ''

// בתבנית:
<h2>${safeGemachName}</h2>
<p>התקבל מאת: <strong>${safeBorrowerName}</strong></p>
${safeNote ? `<p>${safeNote}</p>` : ''}
```

---

### 4. בעיית N+1 בשאילתות 🐢

**בעיה:** כל פונקציה טוענת מחדש את כל הלווים/הלוואות רק לצורך join

**פונקציות מושפעות:**
- `getAllFeePayments()` - טוען כל הלווים וכל ההלוואות
- `getFeePaymentsByBorrower()` - טוען כל הלווים וכל ההלוואות
- `getFeePaymentsByLoan()` - טוען כל הלווים וכל ההלוואות

**פתרון אפשרי 1 - Cache:**
```typescript
let borrowersCache: Map<string, any> | null = null
let loansCache: Map<string, any> | null = null

async function getBorrowersMap(): Promise<Map<string, any>> {
  if (!borrowersCache) {
    const borrowers = await borrowersService.getAll()
    borrowersCache = new Map(borrowers.map(b => [b.id, b]))
  }
  return borrowersCache
}

// לנקות כשצריך:
export function clearFeePaymentsCache() {
  borrowersCache = null
  loansCache = null
}
```

**פתרון אפשרי 2 - Lazy Loading:**
טעינה רק של הרשומות הרלוונטיות:
```typescript
// במקום:
const borrowers = await borrowersService.getAll()
const borrowersMap = new Map(borrowers.map(b => [b.id, b]))

// עשה:
const uniqueBorrowerIds = [...new Set(fees.map(f => f.borrower_id))]
const borrowers = await Promise.all(
  uniqueBorrowerIds.map(id => borrowersService.getById(id))
)
const borrowersMap = new Map(borrowers.map(b => [b.id, b]))
```

---

### 5. Race Condition ב-generateReceiptNumber 🏁

**בעיה:** שתי קריאות במקביל עלולות לקבל אותו מספר קבלה

**תיעוד נוכחי:**
```typescript
/**
 * יצירת מספר קבלה אוטומטי
 * 
 * @warning לא בטוח לקריאות מקבילות - מומלץ לשימוש single-user בלבד
 * במערכת דסקטופ single-user זה מקובל
 */
export async function generateReceiptNumber(): Promise<string> {
  // ... הקוד הקיים
}
```

**פתרון (אם נדרש):**
```typescript
// אופציה 1: שימוש ב-timestamp + random
export async function generateReceiptNumber(): Promise<string> {
  const timestamp = Date.now()
  const random = Math.floor(Math.random() * 1000).toString().padStart(3, '0')
  return `FEE-${timestamp}-${random}`
}

// אופציה 2: שימוש ב-lock (מורכב יותר)
let receiptNumberLock = Promise.resolve()

export async function generateReceiptNumber(): Promise<string> {
  await receiptNumberLock
  const promise = (async () => {
    // הלוגיקה הקיימת
  })()
  receiptNumberLock = promise.catch(() => {})
  return promise
}
```

---

## 📊 סיכום עדיפויות

| # | נושא | חומרה | מאמץ | סטטוס |
|---|---|---|---|---|
| 1 | Audit Log עקביות | בינונית | נמוך | ✅ תוקן |
| 2 | כפילות Audit | נמוכה | נמוך | ✅ תוקן |
| 3 | XSS Escaping | בינונית | נמוך | ⚠️ נדרש |
| 4 | N+1 בעיות ביצועים | נמוכה | בינוני | ℹ️ תיעוד |
| 5 | Race Condition | נמוכה מאוד | נמוך | ℹ️ תיעוד |

---

## המלצות

1. **XSS Escaping** - ⭐ מומלץ לתקן עכשיו (מאמץ קטן, אבזחה טובה)
2. **N+1** - ניתן לדחות, המערכת single-user וקטנה
3. **Race Condition** - ניתן להשאיר עם תיעוד, single-user desktop app

---

**נוצר אוטומטית ע"י סקירת קוד**
