# תיקון בעיית זיהוי הלוואות חסרות באבחון

## הבעיה
המשתמש דיווח: "למה באבחון בעיות הוא לא מוצא חלק מהלוואות שקיימות?"

## הסיבה
הקוד סינן הלוואות לפי `remaining > 0`, אבל לא בדק אם השדה `remaining` בכלל קיים.

### תרחיש בעייתי:
```typescript
const activeLoans = loans.filter((l: any) => l.remaining > 0);
```

אם `l.remaining` הוא `undefined` או `null`, התנאי `undefined > 0` מחזיר `false` וההלוואה לא נספרת.

## הפתרון

### 1. **בדיקה מפורשת של השדה `remaining`**
```typescript
const activeLoans = loans.filter((l: any) => 
  l.remaining !== undefined && 
  l.remaining !== null && 
  l.remaining > 0
);
```

### 2. **מעקב אחרי כל ההלוואות**
הוספנו `allLoansDebug` שרושם את כל ההלוואות לפני הסינון:
```typescript
const allLoansDebug = [];
for (const loan of loans) {
  allLoansDebug.push({
    borrower: `${borrower.first_name} ${borrower.last_name}`,
    amount: loan.amount,
    remaining: loan.remaining,
    status: loan.status,
    hasRemaining: loan.remaining !== undefined && loan.remaining !== null
  });
}
```

### 3. **הודעת שגיאה משופרת**
עכשיו האבחון מראה:
- כמה הלוואות סה"כ נמצאו (גם לא פעילות)
- כמה הלוואות חסר להן `remaining`
- פרטים מלאים על כל ההלוואות (עד 10 ראשונות)

**לפני:**
```
❌ אין לווים עם הלוואות פעילות (remaining > 0)
```

**אחרי:**
```
❌ נמצאו 15 הלוואות אבל אף אחת לא פעילה (remaining > 0)
Data: {
  "totalLoansFound": 15,
  "loansWithoutRemaining": 5,
  "allLoans": [...]
}
```

### 4. **שלב אבחון נוסף (3.5)**
אם יש הלוואות ללא `remaining`, מוצג אזהרה נפרדת:
```
⚠️ נמצאו 5 הלוואות ללא שדה remaining
```

## מה זה אומר למשתמש?

### אם רואים "נמצאו X הלוואות אבל אף אחת לא פעילה":
1. יש לווים במערכת
2. יש להם הלוואות
3. אבל כולן מסומנות כ-"שולם" או שהשדה `remaining` חסר

### פתרונות:
1. ✅ **בדוק שההלוואות לא שולמו במלואן** - אם `remaining = 0` זה תקין
2. ✅ **רענן את הדף (F5)** - אולי הנתונים לא נטענו
3. ✅ **בדוק את שלב 3.5** - אם יש הלוואות ללא `remaining`, זו בעיה בנתונים

## קבצים ששונו
- `src/pages/bank/BankDebugPage.tsx`

## איך לבדוק?
1. פתח את כלי האבחון: **בנק → אבחון בעיות**
2. לחץ **"הרץ אבחון מלא"**
3. בשלב 3 - בדוק את `data.allLoans` בפרטים
4. אם יש שלב 3.5 - יש בעיה בנתונים

## הערות טכניות
- השדה `remaining` מחושב ב-`loansService.getAll()` מהסכום הכולל פחות פירעונות
- אם הלוואה חדשה נוספה ישירות ל-localStorage ללא `remaining`, היא לא תיספר
- רענון הדף (F5) טוען מחדש את כל הנתונים ומחשב `remaining` לכל ההלוואות
