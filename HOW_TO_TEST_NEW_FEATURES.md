# 🔍 כיצד לבדוק את התכונות החדשות

## תאריך: 2026-07-02

---

## ⚠️ חשוב: יש להפעיל מחדש את האפליקציה!

השינויים שביצענו ב-Backend (Rust) דורשים **הפעלה מחדש של האפליקציה** כדי שייכנסו לתוקף.

---

## 📝 מה עדכנו?

### 1. חילוץ שמות משדה `memo` 
- המערכת כעת מחלצת שמות מעסקאות בנק בפורמט: `"המבצע: <שמות>."`
- דוגמה: `"המבצע: בן ציון ופעשא רבקה וורמס."` → `["בן ציון", "פעשא רבקה וורמס"]`

### 2. פיצול שמות מרובים
- פיצול אוטומטי של שמות לפי המילה "ו"
- שמירה על שמות כמו "וורמס" שלמים (לא מפצל אותם בטעות)

### 3. התאמה חלקית של שמות משפחה
- התאמת prefix עם מינימום 3 תווים
- דוגמה: "וורמס" יתאים ל"וורמסר"

### 4. תצוגה משופרת בממשק
- עדיפות לשדה `memo` על פני `description`
- תצוגת השם המלא שחולץ במקום תיאור טכני

---

## 🚀 כיצד להפעיל מחדש?

### אופציה 1: הפעלה רגילה (מומלץ)
```bash
npm run tauri dev
```

### אופציה 2: build ו-run
```bash
cd src-tauri
cargo build
cd ..
npm run tauri dev
```

---

## 🧪 כיצד לבדוק שהתכונות עובדות?

### בדיקה 1: תצוגת עסקאות

1. פתח את האפליקציה
2. עבור לדף "התאמות בנק" (Bank Matching)
3. בדוק את תצוגת העסקאות

**ציפייה:**
- אם לעסקה יש `memo` בפורמט "המבצע: ..." → תראה את השם המחולץ בולט
- אם לעסקה יש `memo` אחר → תראה את ה-memo
- אם אין `memo` → תראה את ה-`description`

---

### בדיקה 2: יצירת התאמות אוטומטיות

1. וודא שיש לך עסקאות ללא התאמה
2. וודא שיש לך לווים עם הלוואות פעילות במערכת
3. לחץ על **"צור התאמות אוטומטיות"**

**ציפייה:**
- המערכת תזהה התאמות גם כאשר:
  - השם ב-memo מכיל מספר שמות ("בן ציון ופעשא רבקה")
  - שם המשפחה מקוטע ("וורמס" במקום "וורמסר")

**בדוק בקונסול (DevTools):**
```
DEBUG: Extracted names from transaction: ["בן ציון", "פעשא רבקה וורמס"]
DEBUG: Target name: בן ציון וורמסר
DEBUG: Transaction memo: Some("המבצע: בן ציון ופעשא רבקה וורמס.")
DEBUG: Name matched! Extracted: 'בן ציון', Target: 'בן ציון וורמסר'
```

---

### בדיקה 3: דיאלוג התאמה ידנית

1. עבור לעסקה ללא התאמה
2. לחץ על **"התאם"** (manual match)
3. חפש שם לווה

**ציפייה:**
- החיפוש ימצא לווים גם אם:
  - השם חלקי
  - יש טעות קלה בשם המשפחה
  - השם מופיע באחת מכמה אפשרויות

---

## 🐛 אם לא רואה התאמות - Debug

### שלב 1: בדוק שהאפליקציה עודכנה
```bash
# בסיום הפעלה, וודא שבקונסול יש:
"DEBUG: Extracted names from transaction: ..."
```

### שלב 2: בדוק את מבנה העסקאות
פתח את Developer Tools (F12) והריץ:
```javascript
const txns = await window.__TAURI__.invoke('get_unmatched_transactions');
console.log('Transactions:', txns);
// בדוק אם יש שדה memo עם תוכן
```

### שלב 3: בדוק את רשימת הלווים
```javascript
const { db } = await import('./src/services/database');
const borrowers = await db.query('SELECT * FROM borrowers WHERE is_deleted = 0');
console.log('Borrowers:', borrowers);
```

### שלב 4: בדוק את הלוגים
- פתח את תיקיית הלוגים של Tauri
- Windows: `%APPDATA%\com.gemach.manager\logs`
- חפש הודעות DEBUG שהדפסנו

---

## 📊 תוצאות צפויות

### עסקה לדוגמה:
```json
{
  "description": "העברה/הפקדה-טל",
  "memo": "המבצע: בן ציון ופעשא רבקה וורמס.",
  "amount": 250,
  "date": "2024-01-15"
}
```

### לווה במערכת:
```json
{
  "first_name": "בן ציון",
  "last_name": "וורמסר",
  "phone": "0501234567"
}
```

### ציון התאמה צפוי:
- **90-95 נקודות** (Excellent)
- סיבות: "סכום מדויק", "תאריך בטווח X ימים", "שם תואם: בן ציון"

---

## ❓ שאלות נפוצות

### Q: למה לא נוצרות התאמות?
**A:** וודא:
1. הפעלת מחדש את האפליקציה אחרי ה-build
2. יש עסקאות עם שדה `memo` מלא
3. יש לווים עם שמות דומים במערכת
4. יתרת ההלוואה גדולה מ-0 (רק הלוואות פעילות)

### Q: למה החיפוש הידני לא מוצא?
**A:** החיפוש הידני משתמש ב-SQL LIKE שלא מיושם עדיין עם התכונות החדשות. התאמות האוטומטיות עובדות מלא!

### Q: איפה הלוגים?
**A:** 
- בקונסול של DevTools (F12)
- בטרמינל שבו הרצת `npm run tauri dev`
- בלוגים של Tauri (ראה שלב 4 ב-Debug)

---

## ✅ Checklist לבדיקה

- [ ] הפעלתי מחדש את האפליקציה
- [ ] רואה שמות מחולצים בתצוגת העסקאות
- [ ] התאמות אוטומטיות נוצרות בהצלחה
- [ ] רואה הודעות DEBUG בקונסול
- [ ] ציוני ההתאמה גבוהים יותר מקודם

---

**מסמך זה נוצר:** 2026-07-02  
**עזרה נוספת:** פנה למפתח עם screenshot של הבעיה + לוגים
